#!/bin/bash

# apply calico pod network
# Create pipeline namespace, dev namespace, and prod namespace
# apply manifests for Jenkins, gogs, docker registry into the pipeline namespace
# apply 'under construction' app to dev namespace, expose on high nodeport
# apply 'under construction' app to prod namespace, expose on port 80

# write readme on how to validate install, create gogs account, push code, create a jenkins job, deploy and see dev, deploy to prod and see prod
